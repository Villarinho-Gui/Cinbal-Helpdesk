import { NextFunction, Request, Response } from 'express'

// interface DataProps {
//   email: string
//   password: string
// }

export function verifyIfJwtIsValid(
  req: Request,
  res: Response,
  next: NextFunction,
) {}
